# Prisma Module

## Purpose
Provides Prisma client wiring for dependency injection.

## Core Tables
All tables are defined in `prisma/schema.prisma` and used by other modules.

## Endpoints
Controller is empty (`prisma.controller.ts`), no public endpoints.

